# if you want to print the value of pi

import math
print("The value of pi is", math.pi)

import statistics
x = [150, 160, 170, 180, 190, 200, 210, 220, 230, 240]
print("The mean is", statistics.mean(x))
print("The median is", statistics.median(x))
print("The mode is", statistics.mode(x))

# Some important libraries are numpy, pandas and matplotlib

# use + sign if you dont want space in print statement

